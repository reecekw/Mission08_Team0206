@model List<Mission08_Team0206.Models.TaskItem>

@{
    ViewData["Title"] = "Quadrants";
}

<!-- Quadrants View: Displays uncompleted tasks in a 2x2 Eisenhower Matrix grid -->
<!-- Each quadrant shows tasks with Edit, Complete, and Delete buttons -->
<h2 class="text-center mb-4">Task Quadrants</h2>

<!-- Top row: Quadrants I and II -->
<div class="row">

    <!-- Quadrant I: Important and Urgent (Red) -->
    <div class="col-md-6 mb-4">
        <div class="card h-100 shadow-sm border-danger">
            <div class="card-header bg-danger text-white fw-bold">
                Quadrant I – Important / Urgent
            </div>
            <div class="card-body">
                @foreach (var task in Model.Where(t => t.Quadrant == 1))
                {
                    <div class="mb-3 p-2 border rounded bg-light">
                        <strong>@task.TaskDescription</strong><br />
                        @if (task.DueDate != null)
                        {
                            <small class="text-muted">Due: @task.DueDate.Value.ToShortDateString()</small><br />
                        }
                        <small class="text-muted">Category: @task.Category?.CategoryName</small>
                        
                        <div class="mt-2">
                            <a asp-action="Edit" asp-route-id="@task.TaskItemId" class="btn btn-sm btn-primary">Edit</a>
                            
                            <form asp-action="MarkCompleted" asp-route-id="@task.TaskItemId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-success">Complete</button>
                            </form>
                            
                            <form asp-action="Delete" asp-route-id="@task.TaskItemId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>

    <!-- Quadrant II: Important and Not Urgent (Yellow) -->
    <div class="col-md-6 mb-4">
        <div class="card h-100 shadow-sm border-warning">
            <div class="card-header bg-warning fw-bold">
                Quadrant II – Important / Not Urgent
            </div>
            <div class="card-body">
                @foreach (var task in Model.Where(t => t.Quadrant == 2))
                {
                    <div class="mb-3 p-2 border rounded bg-light">
                        <strong>@task.TaskDescription</strong><br />
                        @if (task.DueDate != null)
                        {
                            <small class="text-muted">Due: @task.DueDate.Value.ToShortDateString()</small><br />
                        }
                        <small class="text-muted">Category: @task.Category?.CategoryName</small>
                        
                        <div class="mt-2">
                            <a asp-action="Edit" asp-route-id="@task.TaskItemId" class="btn btn-sm btn-primary">Edit</a>
                            
                            <form asp-action="MarkCompleted" asp-route-id="@task.TaskItemId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-success">Complete</button>
                            </form>

                            <form asp-action="Delete" asp-route-id="@task.TaskItemId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>

</div>

<!-- Bottom row: Quadrants III and IV -->
<div class="row">

    <!-- Quadrant III: Not Important and Urgent (Blue) -->
    <div class="col-md-6 mb-4">
        <div class="card h-100 shadow-sm border-info">
            <div class="card-header bg-info text-white fw-bold">
                Quadrant III – Not Important / Urgent
            </div>
            <div class="card-body">
                @foreach (var task in Model.Where(t => t.Quadrant == 3))
                {
                    <div class="mb-3 p-2 border rounded bg-light">
                        <strong>@task.TaskDescription</strong><br />
                        @if (task.DueDate != null)
                        {
                            <small class="text-muted">Due: @task.DueDate.Value.ToShortDateString()</small><br />
                        }
                        <small class="text-muted">Category: @task.Category?.CategoryName</small>
                        
                        <div class="mt-2">
                            <a asp-action="Edit" asp-route-id="@task.TaskItemId" class="btn btn-sm btn-primary">Edit</a>
                            
                            <form asp-action="MarkCompleted" asp-route-id="@task.TaskItemId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-success">Complete</button>
                            </form>

                            <form asp-action="Delete" asp-route-id="@task.TaskItemId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>

    <!-- Quadrant IV: Not Important and Not Urgent (Gray) -->
    <div class="col-md-6 mb-4">
        <div class="card h-100 shadow-sm border-secondary">
            <div class="card-header bg-secondary text-white fw-bold">
                Quadrant IV – Not Important / Not Urgent
            </div>
            <div class="card-body">
                @foreach (var task in Model.Where(t => t.Quadrant == 4))
                {
                    <div class="mb-3 p-2 border rounded bg-light">
                        <strong>@task.TaskDescription</strong><br />
                        @if (task.DueDate != null)
                        {
                            <small class="text-muted">Due: @task.DueDate.Value.ToShortDateString()</small><br />
                        }
                        <small class="text-muted">Category: @task.Category?.CategoryName</small>
                        
                        <div class="mt-2">
                            <a asp-action="Edit" asp-route-id="@task.TaskItemId" class="btn btn-sm btn-primary">Edit</a>
                            
                            <form asp-action="MarkCompleted" asp-route-id="@task.TaskItemId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-success">Complete</button>
                            </form>

                            <form asp-action="Delete" asp-route-id="@task.TaskItemId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>